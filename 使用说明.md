# 题库 · 使用说明

一个**双击就能用**的本地刷题程序。不联网、不装运行库、不上传任何数据。

---

## 一、怎么打开

双击 **`题库.exe`**（或桌面 `工具\题库.lnk`）。

- 用一个**不带浏览器边框**的独立窗口打开界面（系统自带 Edge 内核渲染）
- 关掉窗口，后台的 `题库.exe` **自动退出**，不留常驻进程
- 已经在跑的时候再双击，只会**多开一个窗口**，不会起第二个服务

> ⚠️ **别只拷 `题库.exe`** —— 它要和 `app`、`题库`、`进度` 三个文件夹在一起。整个 `TiKu` 文件夹一起拷。

---

## 二、界面结构

```
┌────────────────────────────────────────────────┐
│ ← 返回   面包屑                        右侧信息  │
├──────────┬─────────────────────────────────────┤
│ 题库列表  │                                     │
│ 导入文件  │             内 容 区                  │
│ 导出文件  │                                     │
│ 学习记录  │                                     │
│ 设置     │                                     │
│ 使用说明  │                                     │
│ ───────  │                                     │
│ 刷新题库  │  ← 在资源管理器里加了文件后点这个      │
└──────────┴─────────────────────────────────────┘
```

进入某一门课后，左侧换成课程菜单：**练习模式 / 真题模拟 / 只练错题 / 收藏题目 / 学习记录 / 本课设置**。

| 课程菜单 | 实际作用 |
| --- | --- |
| 练习模式 | **顺序练习** —— 按原顺序做完整张试卷 |
| 真题模拟 | **随机练习** —— 打乱题目顺序 |
| 只练错题 | 只做做错过的题 |
| 收藏题目 | 只做星标 ☆ 收藏的题 |
| 学习记录 | 本课的统计明细 |
| 本课设置 | 改课程显示名 / 清空本课记录 |

---

## 三、核心概念：一门课 = 一个文件夹

```
题库\
├── 人力资源管理（本科）\        ← 一门课程
│   ├── 2025年1月份真题.txt      ← 一张试卷
│   ├── 2024年4月份真题.txt
│   └── 章节练习（1章）.txt
├── 线性代数（经管类）\          ← 另一门课程
│   └── ...
└── 散装文件.txt                ← 直接放根目录的，归到「未分类」
```

**加课程不用手工建文件夹** —— 在「导入文件」里选「＋ 新建课程」就行。

---

## 四、四个功能怎么用

### 📥 导入文件
1. 选「导入到哪门课程」（或新建一门）
2. **把文件拖进虚线框**，或**点击虚线框选文件**（两种都行）
3. 可一次选多个

> 文件是**复制**进题库文件夹的，原文件不动。
> 格式要求见 `导入格式说明.md`；先打开 `示例课程` 里的示例看一遍最快。

### 📤 导出文件
- 单张试卷「下载」，或「全部导出」（逐个下载）
- 还能**导出答题记录** `state.json`（换电脑时用它恢复进度）

### 📊 学习记录
- **全部课程汇总**：总 XXX 题 / 已做 XXX 题 / 完成率 XX% / 答对 XX / 做错 XX / 正确率 XX%
- **按课程**、**按试卷（章节）** 逐条列出同样的指标
- 口径：**完成率 = 已做 ÷ 总题数**　**正确率 = 答对 ÷ 已做**

### ⚙️ 设置
- **题库根目录** —— 程序默认读 `<程序目录>\题库`，**可以改成任意文件夹**
  （比如让学习助手把题目写在自己的目录里，这里直接指过去，**不用来回拷文件**）
  - 「浏览文件夹…」弹原生选择框；也可以直接粘贴资源管理器地址栏里的路径
  - 「恢复默认」一键回退
  - 约定：**里面每个子文件夹 = 一门课**；直接放根下的文件归「未分类」
- **改课程显示名称** —— 改的是显示名，**不动文件夹名**（更安全，随时能改回来）
- 清空某门课 / 全部课程的答题记录（**不删题目文件**）

---

## 五、答题怎么操作

| 动作 | 鼠标 | 键盘 |
| --- | --- | --- |
| 选选项 | 点选项 | <kbd>1</kbd>–<kbd>8</kbd> 或 <kbd>A</kbd>–<kbd>H</kbd> |
| 上一题 / 下一题 | 底栏按钮 | <kbd>←</kbd> <kbd>→</kbd> |
| 查看答案 | 底栏「查看答案」 | <kbd>空格</kbd> |
| 收藏本题 | 题干右上角 ☆ | — |
| 回课程 | 顶栏「返回」 | <kbd>Esc</kbd> |

- **单选题 / 判断题**：点一下选项**立刻判分**，直接显示对错 + 答案解析
- **多选题**：可以点多个，选完按「提交本题」
- **简答 / 论述 / 案例分析题**：有输入框可以写自己的答案（**写下来记得更牢**）；点「查看答案」后按 **✅ 我答对了 / ❌ 我答错了** 自己判分
- **错题自动进错题本**
- **目录页**：绿=答对　红=答错　紫=主观题自判答对　白=还没做，点题号直接跳

---

## 六、文件都在哪

```
TiKu\
├── 题库.exe          ← 双击这个
├── 使用说明.md
├── 导入格式说明.md
├── app\              ← 界面文件（别动）
├── 题库\             ← 题库数据：一门课一个文件夹
├── 进度\             ← 自动生成
│   ├── state.json              答题记录 + 错题本 + 收藏 + 课程名 + 题量缓存
│   ├── state.日期.bak.json      当天首次的备份
│   ├── 会话令牌.txt             本次运行的身份令牌
│   └── 启动日志.txt             出问题看这个
└── src\              ← 源码（可删，留着方便以后改）
```

- 想备份：把 `题库` + `进度` 两个文件夹拷走
- 想重置：删 `进度\state.json`（或程序里「清空记录」）
- **程序不联网**：所有数据只在本机

---

## 七、出问题怎么办

| 现象 | 处理 |
| --- | --- |
| 双击没反应 | 看 `进度\启动日志.txt` 最后几行 |
| 提示「找不到 Edge 或 Chrome」 | 装一个 Edge（Win11 自带，正常不会缺） |
| 提示「界面没能连上本地服务」 | 多半是**安全软件拦了本地端口**（程序只用 `127.0.0.1`，不对外开放）。放行后重试 |
| 窗口白屏 | 关掉重开；还不行把 `进度\启动日志.txt` 发我 |
| 进度丢了 | `进度\state.日期.bak.json` 是当天备份，改名成 `state.json` 覆盖回去 |
| 某张试卷题量显示 0 | 那张没解析出题目，检查格式；对照 `导入格式说明.md` 改 |

---

## 八、技术说明（给维护的人看）

| 项 | 说明 |
| --- | --- |
| 界面 | 纯 HTML/CSS/JS（`app\` 目录），用 Edge 的 `--app` 模式打开，所以窗口没有地址栏 |
| 启动器 | `src\launcher.cs` → 编译成 `题库.exe`（.NET Framework 4.8，Win11 自带） |
| 编译命令 | `csc /target:winexe /codepage:65001 /r:System.Windows.Forms.dll /out:题库.exe src\launcher.cs` |
| 本地服务 | 只监听 `127.0.0.1`，端口从 45871 起找空的 |
| HTTP 接口 | `GET /api/list`（课程+试卷树）　`GET /api/read?c=&f=`　`POST /api/upload?c=&f=`　`POST /api/mkdir?c=`　`GET/POST /api/state`　`GET /api/alive?t=`　`GET /api/bye?t=` |
| 解析器 | `app\parser.js`，双模式（试卷格式 / 问答清单格式）；自测：`node src/test-parser.mjs [文件]` |
| Word 解析 | 纯前端解 ZIP（`DecompressionStream('deflate-raw')`）+ 正则取 `word/document.xml` 正文，不依赖任何库 |
| .doc | **不支持**老 `.doc`，必须 `.docx` |

### 🔴 三条不能改的设计（都是踩坑换来的）

1. **生命周期靠心跳，不靠 `Process.WaitForExit()`**
   Edge 启动会做**进程交接**，`Process.Start` 拿到的句柄可能几百毫秒就退出，`WaitForExit()` 会提前返回、把服务关掉，页面变成「无法访问」。
   → 现在：页面每 2 秒 `GET /api/alive`，关窗口时 `sendBeacon('/api/bye')`，兜底超时 150 秒。

2. **心跳必须带会话令牌（`?t=`）**
   不然上一次会话残留的旧标签页被回收时会发 `/api/bye`，**把新服务误杀**（实测：新实例起来 20 秒后被杀）。
   → 每次启动生成随机令牌，只写进本次打开的 URL；令牌对不上一律忽略。

3. **章节标题只认中文数字编号**
   否则 `8.简述工作分析的基本流程。` 会被当成 `三、简答题` 那种大标题吞掉。
   → 而且**题号判断必须排在标题判断前面**。
